---
import Base from '../../layouts/Base.astro';
---
<Base title="Radiance Cascades GI" desc="级联辐射度 2D 全局光照：画光源画墙，实时软阴影与半影">
<main class="wrap reader">
  <a class="back mono" href="/lab/">← 实验室</a>
  <h1 class="rd-title">Radiance Cascades GI</h1>
  <div class="meta mono">
    <span class="sw" style="background:var(--c-ai);"></span>
    <span>WebGPU · 2D 全局光照</span>
    <span>裸 API，无框架</span>
  </div>

  <div class="lab-stage">
    <canvas id="lab-cv"></canvas>
    <div class="lab-hud mono" id="lab-hud">正在初始化 WebGPU…</div>
  </div>
  <p class="mono" id="lab-nogpu" hidden style="color:var(--c-render); max-width:800px;"></p>

  <div class="lab-ctl mono">
    <label>画笔
      <select id="rc-brush">
        <option value="0" selected>光源</option>
        <option value="1">墙</option>
        <option value="2">橡皮</option>
      </select>
    </label>
    <label>色相 <input id="rc-hue" type="range" min="0" max="1" step="0.01" value="0.08" /></label>
    <label>笔刷 <input id="rc-size" type="range" min="3" max="30" step="1" value="10" /></label>
    <label>视图
      <select id="rc-view">
        <option value="0" selected>全局光照</option>
        <option value="1">场景（发光体/墙）</option>
        <option value="2">JFA 距离场</option>
        <option value="3">Cascade 0 原始</option>
      </select>
    </label>
    <button type="button" class="pie-btn" id="rc-clear">清空</button>
  </div>
  <p style="max-width:800px; color:var(--ink2); font-size:14px;">
    直接在画布上画：光源发光、墙挡光，光照当帧收敛，没有噪点也不需要
    时域累积。这是 Sannikov 的 Radiance Cascades——利用「半影条件」：
    离光源越远，光照的<b>角度频率</b>越低而<b>空间频率</b>越高。于是把
    辐射度按距离切成 5 层级联：cascade 0 探针每 2px 一个但只看 4 个方向、
    只管眼前 4px；每升一级探针间距 ×2、方向数 ×4、射线区间 ×4——每级
    内存恒定。自顶向下归并后 cascade 0 就是完整 GI。射线在 JFA 跳跃泛洪
    算出的距离场上做球形步进。切「JFA 距离场」和「Cascade 0 原始」视图
    能看到这套机器的两块底层零件。
  </p>

  <details style="max-width:800px;">
    <summary class="mono" style="cursor:pointer; color:var(--ink2);">WGSL 源码（级联行进与归并）</summary>
    <pre class="mono" id="lab-wgsl" style="border:1px solid var(--line); background:var(--surface); padding:14px 16px; overflow-x:auto; font-size:12px; line-height:1.6;"></pre>
  </details>
</main>
</Base>

<script>
  import '../../scripts/lab/radiance.js';
</script>
