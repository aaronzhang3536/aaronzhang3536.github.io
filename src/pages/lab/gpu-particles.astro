---
import Base from '../../layouts/Base.astro';
---
<Base title="GPU 粒子" desc="裸 WebGPU：compute shader 驱动的百万级粒子，附真实 GPU 计时">
<main class="wrap reader">
  <a class="back mono" href="/lab/">← 实验室</a>
  <h1 class="rd-title">GPU 粒子</h1>
  <div class="meta mono">
    <span class="sw" style="background:var(--c-render);"></span>
    <span>WebGPU · Compute Shader</span>
    <span>裸 API，无框架</span>
  </div>

  <div class="lab-stage">
    <canvas id="lab-cv"></canvas>
    <div class="lab-hud mono" id="lab-hud">正在初始化 WebGPU…</div>
  </div>
  <p class="mono" id="lab-nogpu" hidden style="color:var(--c-render); max-width:800px;"></p>

  <div class="lab-ctl mono">
    <label>粒子数 <input id="lab-count" type="range" min="12" max="22" step="1" value="18" />
      <b id="lab-count-v" style="display:inline-block; min-width:74px;">262,144</b>
    </label>
    <label>引力 <input id="lab-grav" type="range" min="-2" max="7" step="0.1" value="3" /></label>
    <label>湍流 <input id="lab-turb" type="range" min="0" max="3" step="0.05" value="1.2" /></label>
    <label>拖尾 <input id="lab-trail" type="range" min="0.02" max="0.5" step="0.01" value="0.12" /></label>
  </div>
  <p style="max-width:800px; color:var(--ink2); font-size:14px;">
    移动鼠标牵引粒子群；离开画布后引力点自动漫游。模拟在 compute shader
    里完成（引力 + 双频正弦扰流 + 阻尼），渲染是每粒子 6 顶点的屏幕空间
    billboard，加色混合叠进离屏累积缓冲做拖尾，最后 blit 上屏。左上角的
    毫秒数来自 <code>timestamp-query</code> —— 是这块 GPU 真实的 pass 耗时，不是估的。
  </p>

  <details style="max-width:800px;">
    <summary class="mono" style="cursor:pointer; color:var(--ink2);">WGSL 源码</summary>
    <pre class="mono" id="lab-wgsl" style="border:1px solid var(--line); background:var(--surface); padding:14px 16px; overflow-x:auto; font-size:12px; line-height:1.6;"></pre>
  </details>
</main>
</Base>

<script>
  import '../../scripts/lab/gpu-particles.js';
</script>
